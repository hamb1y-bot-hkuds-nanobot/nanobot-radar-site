services:
  web:
    build: .
    image: nanobot-radar:latest
    container_name: nanobot-radar
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:80"
    read_only: true
    tmpfs:
      - /var/cache/nginx
      - /var/run
    security_opt:
      - no-new-privileges:true
